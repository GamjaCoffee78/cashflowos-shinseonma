/node_modules
/.next
/out
/build
next-env.d.ts

# secrets — NEVER commit these
.env
.env*.local
*.env
*.key
*.bak
*secret*
# but DO keep the example
!.env.example

*.tsbuildinfo
.DS_Store
.vercel

# business data — NEVER commit real customer files to a public repo.
# Your numbers belong in Supabase, not in git.
# THIS REPO IS PUBLIC: anything committed here is readable by anyone on earth.
*.csv
*.xlsx
*.xls
*.numbers
*.pdf
# /data holds your real okmaya figures for `npm run import`. Never tracked.
/data/

# generated SQL loads with real figures — never commit
*.sql
!supabase/schema.sql
