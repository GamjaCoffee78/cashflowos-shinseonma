/node_modules
/.next
/out
/build
next-env.d.ts

# secrets — NEVER commit these
.env
.env*.local
*.env
*.key
*.bak
*secret*
# but DO keep the example
!.env.example

*.tsbuildinfo
.DS_Store
.vercel

# business data — NEVER commit real customer files to a public repo.
# Your numbers belong in Supabase, not in git. Already-tracked files are
# unaffected, which is why the template's sample CSV below still works.
*.csv
*.xlsx
*.xls
*.numbers
*.pdf
# but DO keep the template's sample import
!docs/sample-import.csv
